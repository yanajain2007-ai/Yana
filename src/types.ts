export interface SocialStat {
  platform: string;
  followers: string;
  engagement: string;
  icon: string;
}

export interface Project {
  id: number;
  title: string;
  category: string;
  imageUrl: string;
}

export interface Brand {
  name: string;
  logo: string;
}
